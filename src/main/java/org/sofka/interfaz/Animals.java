package org.sofka.interfaz;

public interface Animals {
    /**
     * the sound of an animal returns
     * @param age receives the age of the pet
     */
     int realAge(int age);
    /**
     * return action pets
     * @return  action pets
     */
     String actionPets();
}
